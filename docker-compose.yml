version: '3.9'

services:
  mongo:
    image: mongo:7.0
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

  api:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev:server"
    ports:
      - '3333:3333'
    environment:
      NODE_ENV: development
      PORT: '3333'
      HOST: 0.0.0.0
      MONGODB_URI: mongodb://mongo:27017/my-abeka-calendar
      CORS_ORIGIN: http://localhost:5173
    volumes:
      - .:/app
      - api-node-modules:/app/node_modules
    depends_on:
      - mongo

  web:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    ports:
      - '5173:5173'
    environment:
      NODE_ENV: development
      VITE_API_URL: http://localhost:3333
    volumes:
      - .:/app
      - web-node-modules:/app/node_modules
    depends_on:
      - api

volumes:
  mongo-data:
  api-node-modules:
  web-node-modules:

