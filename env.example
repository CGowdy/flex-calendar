NODE_ENV=development
PORT=3333
HOST=0.0.0.0

# For dev (DB-only in Docker):
# Use root credentials from .env.mongo and connect to localhost.
MONGODB_URI=mongodb://root:example@localhost:27017/my-abeka-calendar?authMechanism=DEFAULT

# CORS is optional in dev because Vite proxies /api to the API server.
# Leave unset unless you access the API directly from a different origin.
# CORS_ORIGIN=http://localhost:5173

# Frontend: by default uses Vite proxy to /api.
# Set only if you want a full absolute API URL in the browser.
# VITE_API_URL=http://localhost:3333/api

